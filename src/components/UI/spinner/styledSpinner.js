import styled from 'styled-components/native'

const SpinnerWrapper = styled.View`
	flex: 1;
	align-items: center;
	justify-content: center;
`
const SpinnerIcon = styled.ActivityIndicator``

export { SpinnerWrapper, SpinnerIcon }
